# Frontend — Distributed Job Scheduler Dashboard

React + Vite dashboard for the job scheduler API. See the [top-level README](../README.md) for full setup instructions.

```bash
npm install
cp .env.example .env   # adjust VITE_API_BASE_URL if the API isn't on localhost:4000
npm run dev
```
